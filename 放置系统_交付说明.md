# 任务4 交付说明：指定位置放置物品（ItemPlacement）

- Unity 版本：6000.6.0f1
- 状态：脚本已完成，场景已由配置工具增量配置，自动化验收 **37/37 通过**（见 `放置系统_验收日志.md`），打开 SC_Bedroom 场景直接 Play 即可测试
- 范围：仅指定位置放置 + 放置成功通知；**未开始章节开发（ChapterManager 未创建，仅预留事件挂钩）**
- 本次调整：已回退旧版自造的鱼缸方块（Tank_Body）、放置标记（PlacementMarker）、FishTankArea 层级及相关材质；
  放置点改为使用场景中**已存在的真实道具 GlassBowl（玻璃鱼缸）与 MyBedroomBookshelf（书架）**，小盒放在书架顶、鱼缸旁边，**不再自造任何可见物件**。

---

## 1. 修改文件清单

### 1.1 新增（运行时脚本，随项目编译）

| 文件 | 职责 |
| ---- | ---- |
| `Assets/Game/Scripts/Items/ItemPlacement.cs` | 放置点组件（继承 `InteractableBase`）：配置 `requiredItemId` / `displayObject` / `playerInventory` / `interactionCollider` / 提示文案；条件全满足才消耗物品；成功后显示展示物件并触发 `ItemPlaced` 事件 |

### 1.2 新增（Editor 场景配置工具）

| 文件 | 职责 |
| ---- | ---- |
| `Assets/Game/Editor/ItemPlacementSetup.cs` | 一键配置书架放置点：菜单 `Tools/交互系统/配置任务道具放置（任务4）`，幂等可重复执行；内含旧版本对象的回退逻辑 |
| `Assets/Game/Editor/Harness/ItemPlacementHarness.cs` | Play 模式自动化验收（InputTestRuntime 驱动 E 键），写出 `放置系统_验收日志.md` |

### 1.3 场景修改（无新预制体/新材质）

| 场景 | 说明 |
| ---- | ---- |
| `Assets/Game/Scenes/SC_Bedroom.unity` | 在已有的 MyBedroomBookshelf 下增量加入 SmallBoxPlacement 层级（见第 2 节） |

### 1.4 已回退（旧版本自造物件）

- `FishTankArea` 整棵层级（含 FishTank_Model / Tank_Body / PlacementMarker / 旧放置点）已删除；
- 材质 `Assets/Game/Prefabs/M_TankGlass.mat`、`M_PlacementSpot.mat` 已删除；
- 一次性诊断脚本 DiagReportGeometry.cs 已删除。

### 1.5 修改

- 任务4 本体未修改任何既有脚本（FirstPersonController / InteractableBase / PickupItem / Inventory / UI 全部原样复用）。
- **后续微调（仅射线检测，2026-09-21）**：`PlayerInteractor.cs` 的中心查询由单条无限细 `Physics.Raycast` 改为默认 0.03m 小半径 `Physics.SphereCastNonAlloc`，结果按距离排序后取最近的 `InteractableBase`。
  - 原因：小盒紧挨门时，准星擦过盒边细射线会落到门上，导致看向盒子无法拾取。
  - 实测可瞄准角度窗口由约 ±6.6° 扩大到 ±9.3°；真正被门遮挡时仍正确选门；半径设为 0 即恢复原细射线行为。
  - 仅新增一个 Inspector 字段 `castRadius`（默认 0.03），输入、UI、交互触发逻辑均未改动；37 项验收复测仍全部通过。
- **后续微调（关门交互，2026-09-21）**：门打开后无法关门，两处根因均在 `PlayerInteractor.cs`，已最小修复：
  1. 开门时 `DoorInteractable` 会把门板碰撞体设为 Trigger（开门不挡路），而检测默认忽略 Trigger → 开门后找不到门。修复：主检测无目标时，追加一次“允许 Trigger、且只接受挂了 InteractableBase 的命中”的回退查询；隐形非交互触发区仍不会被选中。
  2. 门开/关是同一对象，目标未切换时 `SetTarget` 不刷新提示 → 提示停留在 Open Door。修复：目标不变但 `GetInteractionPrompt()` 文案变化时也刷新 UI。
  - 门专项验收 **9/9 通过**（见 `门交互_验收日志.md`）：关门提示 Open → 按 E 开门（Trigger）→ 仍可瞄准、提示变 Close → 按 E 关门（恢复实体）。
  - 未修改 `DoorInteractable.cs`。
- **历史记录（现已解决）**：本任务最初验收时，任务5 的 `ChapterManager.lostPetPlacement` 尚未绑定。当前场景已将它绑定到 `SmallBoxPlacement`；此段仅保留用于解释旧日志，不代表当前仍有该报错。

---

## 2. Unity 场景配置（层级结构）

放置点挂在场景已有的书架之下，只创建交互必需的最小对象：

```
MyBedroomBookshelf                         （场景已有，书架）
└── SmallBoxPlacement                      （挂 ItemPlacement 组件；位于书架顶、鱼缸旁边）
    ├── InteractionCollider                （Interactable 层，实体 BoxCollider 0.25m，隐形射线目标）
    └── PlacedSmallBox                     （展示物件，初始隐藏）
        └── SmallBox_Model                 （0.25m 立方体，材质 M_SmallBox，无碰撞体，纯展示）

GlassBowl                                  （场景已有，玻璃鱼缸，放在书架顶上）
```

实测世界坐标（书架/鱼缸同属一个父节点，父节点自身有偏移）：

| 对象 | 世界位置 | 说明 |
| ---- | ---- | ---- |
| GlassBowl | (-3.85, 0.978, -0.98) | 鱼缸底面 y=0.978 即书架顶面 |
| SmallBoxPlacement | (-3.86, 1.10, -1.30) | 鱼缸正后方 0.32m，同处书架顶面 |
| 书架顶面范围 | x∈[-4.08,-3.63]，z∈[-1.55,-0.71] | 小盒完整落在范围内 |

> **组件挂载说明**：`ItemPlacement` 挂在 `SmallBoxPlacement` 根节点（InteractionCollider 的父级）。
> PlayerInteractor 用 `hit.collider.GetComponentInParent<InteractableBase>()` 从碰撞体向上查找，
> 组件在根节点才能被命中；挂在 InteractionCollider 的兄弟节点上则无法被查找。

### 方式 A：一键配置（已完成，可重复执行）

1. 打开场景 `Assets/Game/Scenes/SC_Bedroom.unity`。
2. 菜单 **Tools → 交互系统 → 配置任务道具放置（任务4）**。
3. 控制台输出“配置完成并已保存场景”即可。工具幂等：已存在的物体会被复用/重建；
   若 Player 上缺 `Inventory` 或缺 `Pickup_SmallBox`，会先自动调用任务3的配置工具补全；
   旧版本自造的鱼缸/标记会被自动回退。

### 方式 B：手动配置（与工具行为等价，供核对/复现）

1. 在场景中确认 `MyBedroomBookshelf` 与 `GlassBowl`（鱼缸已放在书架顶上）。
2. 在 `MyBedroomBookshelf` 下建空物体 `SmallBoxPlacement`，位置取书架顶、鱼缸旁边（盒底与鱼缸底面同高 y≈0.978，盒中心 y≈1.10）。
3. `SmallBoxPlacement` 下建 `InteractionCollider`：
   - 挂 `BoxCollider`：Size (0.25, 0.25, 0.25)，Center (0, 0, 0)，**isTrigger 必须为 false**（PlayerInteractor 配置为忽略 Trigger）；
   - **Layer 设为 `Interactable`（含子物体）**。
4. `SmallBoxPlacement` 下建 `PlacedSmallBox`，**取消勾选 active**（初始隐藏）；
   - 其下建 `SmallBox_Model`：Cube，Scale (0.25, 0.25, 0.25)，材质拖 `M_SmallBox`，删除 Collider（纯展示）。
5. 给 `SmallBoxPlacement` 挂 `ItemPlacement`，按第 3 节填 Inspector。
6. 保存场景。

---

## 3. Inspector 引用表

| 物体 | 组件 | 字段 | 配置值 | 说明 |
| ---- | ---- | ---- | ---- | ---- |
| SmallBoxPlacement | ItemPlacement | requiredItemId | SmallBox | 放置所需物品 |
| SmallBoxPlacement | ItemPlacement | displayObject | PlacedSmallBox | 成功后才显示，初始隐藏 |
| SmallBoxPlacement | ItemPlacement | playerInventory | Player 的 Inventory | Inspector 明确绑定，运行时不搜索 |
| SmallBoxPlacement | ItemPlacement | promptFormat | `E - Place {0}` | 拥有物品时的提示，{0}=物品名 |
| SmallBoxPlacement | ItemPlacement | lockedPromptFormat | `Need {0}` | 没有物品时的提示 |
| SmallBoxPlacement | ItemPlacement | interactionCollider | InteractionCollider 的 BoxCollider | 成功后禁用，停止再次交互 |
| InteractionCollider | (GameObject) | Layer | Interactable (6) | 射线可命中 |
| PlacedSmallBox | (GameObject) | active | false（初始） | 放置成功前不可见 |

> **提示文案说明**：需求原文为「E 放置小盒」。项目默认 TMP 字体（LiberationSans SDF）不含中文字形，
> 中文会显示为方块，故默认配置为英文 `E - Place SmallBox`。导入中文字体后把 `promptFormat` 改为
> `E - 放置 {0}` 即得需求原文提示（字体接入属于后续任务，不在本期范围）。

---

## 4. 手动测试步骤（验收）

### 4.1 正常流程

1. 打开场景点击 **Play**：物品栏 3 槽为空；走到书架前（GlassBowl 鱼缸在书架顶上）。
2. 没有小盒时，对准书架顶鱼缸旁边的位置 → 提示 `Need SmallBox`；按 **E** → 无任何变化（日志“放置失败（背包中没有指定物品…）”，物品栏不变）。
3. 走到 `Pickup_SmallBox` 前对准 → `E - Pick up SmallBox`，按 **E** → 小盒消失、背包槽 1 显示 `SmallBox`。
4. 回到书架前，对准鱼缸旁边 → 提示变为 `E - Place SmallBox`；按 **E** → 书架顶、鱼缸旁出现小盒（PlacedSmallBox），背包清空。
5. 再次对准该位置按 **E** → 无任何变化（放置点交互已禁用，日志提示不能再交互）。

### 4.2 边界情况逐条验证

| # | 场景 | 预期结果 |
| - | ---- | ---- |
| 1 | 没有小盒时对准放置点按 E | 不消耗、不放置、不显示展示物件，物品栏不变 |
| 2 | 拾取小盒后按 E | 放置成功：IsPlaced=true、背包移除、展示物件显示 |
| 3 | 放置后再次按 E（含连续快按） | 不重复消耗、不重复生成、事件只触发一次 |
| 4 | 放置后交互碰撞体被禁用 | 移开再对准，放置点不再被射线命中，提示消失 |
| 5 | 未配置 requiredItemId | CanInteract=false，控制台报错提示未配置 |
| 6 | 未绑定 playerInventory | 首次交互报错并拒绝放置（防御：Inspector 未绑定且拿不到交互者时 CanInteract=false） |
| 7 | 事件通知 | `ItemPlacement.ItemPlaced`（参数 ItemId）放置成功后触发一次，供 ChapterManager 订阅 |

### 4.3 关于“原来的拾取小盒交互去哪了”

拾取交互一直存在且未被修改：`Pickup_SmallBox`（Interactable 层、BoxCollider、PickupItem 组件）
在场景中保持激活，验收第 22–24 项实测通过（显示 `E - Pick up SmallBox`、按 E 后入包、场景物件隐藏）。
之前看起来“找不到”，是因为旧版自造的鱼缸方块紧挨着拾取小盒、视觉上把它淹没；回退方块后已恢复清晰。

---

## 5. ChapterManager 接入说明（本期不做，仅预留挂钩）

`ItemPlacement` 不调用金鱼特效、不播放章节结局、不做运行时 `FindObjectsOfType` 搜索管理器；
放置成功只通过 **事件** 通知。章节脚本订阅方式：

```csharp
placement.ItemPlaced += OnItemPlaced;

private void OnItemPlaced(ItemId itemId)
{
    // itemId == ItemId.SmallBox 时推进章节流程
}
```

引用 `placement` 建议在 Inspector 拖入（与 `playerInventory` 等一致，避免运行时搜索）。

---

## 6. 架构与需求对应

| 需求 | 实现 |
| ---- | ---- |
| 复用现有系统，不重新实现 | 继承 `InteractableBase`，复用 PlayerInteractor 射线与 E 键、Inventory 移除、PickupItem 拾取 |
| Inspector 配置 requiredItemId | `[SerializeField] private ItemId requiredItemId` |
| Inspector 配置展示物件、初始隐藏 | `displayObject` 字段；场景 inactive + `Awake` 防御性隐藏 |
| 没有指定物品时不允许放置 | `CanInteract()` 返回 false（无物品/未配置/已放置），按 E 走失败反馈，零副作用 |
| 拥有物品时提示放置 | `GetInteractionPrompt()` 有物品 → `E - Place {0}`；无物品 → `Need {0}` |
| 放置成功后从 Inventory 移除 | 条件全满足后才 `TryRemoveItem` |
| 同时显示展示物件 | 移除成功后才 `displayObject.SetActive(true)` |
| 只能成功一次 | `IsPlaced` 状态 + 成功后禁用交互碰撞体双保险 |
| 明确的放置成功通知 | `public event Action<ItemId> ItemPlaced`（成功时 Invoke，参数为物品 Id） |
| 只有全部条件满足才消耗物品 | 顺序：IsPlaced 防御 → Id 配置 → Inventory 存在 → HasItem → TryRemoveItem，任一失败即返回 |
| 已放置不重复消耗 | `Interact()` 首行 `if (IsPlaced) return;` + CanInteract 拦截 |
| 物品不存在不显示展示物件 | 展示物件仅在 TryRemoveItem 成功之后启用 |
| 失败操作不改物品栏/放置状态 | 所有失败路径在任何修改之前 return；日志区分原因 |
| 不调用金鱼特效、不播放结局 | 无任何特效/章节引用 |
| 通过事件通知 ChapterManager | `ItemPlaced` 事件；不创建/不引用 ChapterManager |
| 不用 FindObjectsOfType 搜索管理器 | 运行时组件只经 Inspector 绑定 + 交互者提供 Inventory；场景配置由 Editor 工具完成 |
| 优先 Inspector 绑定引用 | `playerInventory`/`displayObject`/`interactionCollider` 均由配置工具绑定 |
| 不自造可见物件 | 鱼缸用场景已有的 GlassBowl；只创建隐形碰撞体与展示小盒（小盒为任务本体） |

---

## 7. 验证情况

- **编译**：Unity 6000.6.0f1 batchmode 全量导入，`Assembly-CSharp` 0 错误（退出码 0）。
- **回退**：FishTankArea 层级、Tank_Body、PlacementMarker、M_TankGlass.mat、M_PlacementSpot.mat、DiagReportGeometry.cs 全部删除（验收 6–8 项确认场景中不存在）。
- **运行时验收**：Play 模式 + `InputTestRuntime` 驱动 E 键，**37/37 PASS**，详见根目录 `放置系统_验收日志.md`：
  - 场景接线：关键对象齐全、绑定正确、放置点挂在书架下；
  - 几何：小盒与鱼缸底面同高（y=0.978）、水平相邻（0.32m）、不重叠、落在书架顶面范围内；
  - 主流程：无小盒显示 `Need SmallBox` → 按 E 无副作用 → 拾取（拾取交互完整）→ 回书架提示 `E - Place SmallBox`
    → 放置成功（IsPlaced、背包移除、展示物件显示、事件一次、碰撞体禁用）→ 再次按 E 无重复消耗/生成；
  - 全程无 Error/Exception。
- 历史验证缺口：本任务验收当时中文字体尚未接入；当前已存在 `Assets/Game/Resources/MemoryFontSerif.asset`，字体的最终显示效果仍应在当前版本 Smoke Test 中确认。

---

## 8. 剩余风险与后续

| 风险/事项 | 说明 | 建议 |
| ---- | ---- | ---- |
| 放置点为书架上的临时位置 | 当前在鱼缸正后方 0.32m | 如需调整，移动 `SmallBoxPlacement`（InteractionCollider/PlacedSmallBox 为子物体自动跟随），再按 E 验证 |
| 放置节点是书架预制体实例的“新增对象” | Unity 预制体实例允许添加子物体；若对书架执行 Revert/替换模型会丢失该节点 | 替换书架模型后重新执行一次配置工具即可 |
| 中文提示未启用 | 默认 TMP 字体无中文字形 | 接入中文字体后改 `promptFormat = "E - 放置 {0}"` |
| InteractionCollider 为实体碰撞体 | PlayerInteractor 忽略 Trigger，必须实体；位于书架顶不挡路 | 不得改为 Trigger（否则射线忽略） |
| 放置成功事件 | 已预留 `ItemPlaced`，ChapterManager 未实现 | 章节任务接入时订阅事件推进流程 |

---

## 9. 暂不实现（明确范围外）

自由摆放、拖拽放置、物理掉落、物品旋转、复杂组合配方、Shader 和动画、章节结局播放、金鱼特效联动、存档/读档。
