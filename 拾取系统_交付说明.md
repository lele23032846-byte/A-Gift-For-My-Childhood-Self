# 任务3 交付说明：任务道具拾取与最小物品栏

- Unity 版本：6000.6.0f1
- 状态：脚本已完成，场景已由配置工具增量配置，**打开 SC_Bedroom 场景直接 Play 即可测试**
- 范围：仅拾取 + 最小物品栏；**未实现放置系统（ItemPlacement）**

---

## 1. 修改文件清单

### 1.1 新增（运行时脚本，随项目编译）

| 文件 | 职责 |
| ---- | ---- |
| `Assets/Game/Scripts/Items/ItemId.cs` | 稳定的任务物品标识（枚举，None=0 保留，真实物品从 1 起；含 `GetDisplayName()` 展示名映射） |
| `Assets/Game/Scripts/Items/Inventory.cs` | 物品栏：默认容量 3、任务道具唯一、查询/添加/移除、事件通知 |
| `Assets/Game/Scripts/Items/PickupItem.cs` | 可拾取物（继承 `InteractableBase`）：按 E 先添加、成功才隐藏 |
| `Assets/Game/Scripts/UI/InventoryHUD.cs` | 最简物品栏 UI：只读展示，订阅事件刷新，不每帧轮询 |

### 1.2 新增（Editor 场景配置工具）

| 文件 | 职责 |
| ---- | ---- |
| `Assets/Game/Editor/InventorySystemSetup.cs` | 一键增量搭建：菜单 `Tools/交互系统/配置物品拾取与物品栏（任务3）`，幂等可重复执行 |

### 1.3 配置工具自动生成（运行时产物）

| 产物 | 说明 |
| ---- | ---- |
| `Assets/Game/Prefabs/Pickup_SmallBox.prefab` | 小盒预制体（含 PickupItem、itemId=SmallBox、Interactable 层、Collider），便于复制出“两个相同 Id 物件”测试 |
| `Assets/Game/Prefabs/M_SmallBox.mat` | 小盒木纹材质（URP Lit） |

### 1.4 修改

- **未修改任何既有脚本**。
- 场景 `Assets/Game/Scenes/SC_Bedroom.unity` 由配置工具增量加入：Player 上挂 `Inventory`、场景新增 `Pickup_SmallBox`、新增 `UI_Inventory` 画布（3 槽）。

---

## 2. Unity 挂载步骤

### 方式 A：一键配置（推荐，已完成一次，可重复执行）

1. 打开场景 `Assets/Game/Scenes/SC_Bedroom.unity`。
2. 菜单 **Tools → 交互系统 → 配置物品拾取与物品栏（任务3）**。
3. 控制台输出“配置完成并已保存场景”即可。工具幂等：已存在的物体/组件/UI 会被复用，不重建。

### 方式 B：手动配置（与工具行为等价，供核对/复现）

1. **Player 挂物品栏**：选中 `Player` → Add Component → `Inventory`（Capacity 保持 3）。
2. **创建小盒**：Hierarchy 右键 → 3D Object → Cube，命名 `Pickup_SmallBox`；
   - Scale ≈ (0.25, 0.25, 0.25)，放在玩家面前可及处；
   - **Layer 设为 `Interactable`（含子物体）**，确保有 Collider（Cube 自带 Box Collider，勿设 Trigger）；
   - Add Component → `PickupItem`，itemId 下拉选择 `SmallBox`（visualRoot 留空 = 拾取后隐藏自身）；
   - 建议右键拖入 `Assets/Game/Prefabs/` 保存为预制体，方便复制第二盒。
3. **创建物品栏 UI**：
   - 创建 Canvas 命名 `UI_Inventory`（Render Mode: Screen Space Overlay + CanvasScaler 1920×1080）；
   - 其下建 `SlotsPanel`（HorizontalLayoutGroup，锚定右上角），建 3 个槽位 `Slot_0 / Slot_1 / Slot_2`（半透明 Image 底 + 子物体 `Label` 用 TextMeshProUGUI，初始文本 `—`）；
   - Canvas 上挂 `InventoryHUD`：`inventory` 拖 Player 的 `Inventory`，`slotTexts` 依次拖 3 个 Label。
4. 保存场景。

> 提示：PlayerInteractor / 交互提示 UI 来自上一任务，已就绪，本任务不涉及修改。

---

## 3. Inspector 配置表

| 物体 | 组件 | 字段 | 配置值 | 说明 |
| ---- | ---- | ---- | ---- | ---- |
| Player | Inventory | capacity | 3 | 背包容量 |
| Pickup_SmallBox | (GameObject) | Layer | Interactable (6) | 射线可命中 |
| Pickup_SmallBox | PickupItem | itemId | SmallBox | 稳定物品标识 |
| Pickup_SmallBox | PickupItem | visualRoot | （空） | 空 = 拾取后隐藏本物体 |
| Pickup_SmallBox | PickupItem | promptFormat | `E - Pick up {0}` | {0} 替换为物品显示名 |
| UI_Inventory | InventoryHUD | inventory | Player 的 Inventory | 只读数据源；留空自动查找 |
| UI_Inventory | InventoryHUD | slotTexts | Slot_0/1/2 的 Label | 固定 3 槽，与容量一致 |
| UI_Inventory | InventoryHUD | emptySlotLabel | — | 空槽占位文本 |

---

## 4. 手动测试步骤（验收）

### 4.1 正常流程

1. 打开场景点击 **Play**：玩家面前有小盒，右上角物品栏 3 个空槽显示 `—`。
2. 准星对准小盒 → 屏幕下方提示 `E - Pick up SmallBox`。
3. 按 **E** → 场景小盒消失；物品栏 **槽 1 显示 `SmallBox`**（槽 2、3 仍为 `—`）。
4. 观察 Console：无报错（仅拾取日志）。

### 4.2 边界情况逐条验证

| # | 场景 | 预期结果 |
| - | ---- | ---- |
| 1 | 连续快速按 E（连点 5~10 下） | 只拾取一次，背包仍 1 件；小盒已消失、射线不再命中 |
| 2 | 同一个物件重复拾取 | 物已消失无法再按 E；即使直接调用交互逻辑，`TryAddItem` 也拒绝重复（唯一道具） |
| 3 | 背包已满 | 拾取失败、小盒**保留**在场景，控制台打印“背包已满”；物品栏不变 |
| 4 | 拾取过程中移开视角 | 按下瞬间完成拾取，无中间态；移开后目标消失、不触发 |
| 5 | 场景存在两个相同 ItemId 的物件 | 复制 `Pickup_SmallBox.prefab` 放第二盒：拾取第一盒后消失；对准第二盒按 E → 被拒（已拥有），第二盒保留 |
| 6 | 拾取成功物品栏更新 | 槽 1 立即变为 `SmallBox`（`Inventory.Changed` 事件驱动，非每帧刷新） |

> **背包已满的复现说明**：当前仅注册 `SmallBox` 一种物品，无法用两个不同 Id 自然填满 3 格。两种复现方式：
> - 快速验证：把 `Player/Inventory.capacity` 临时改为 `0` → 按 E 拾取即失败并打印“背包已满”（拾取后改回 3）；
> - 完整复现：按第 5 节注册第二种物品后，先拾取第一件、再填满背包、再对准第二件拾取，即命中“满”分支。

---

## 5. 新增道具（后续任务用）

1. `ItemId.cs` 枚举**末尾追加**新值（如 `Photo = 2`，禁止重排/复用旧值，值会进存档）；
2. `GetDisplayName()` 增加对应 `case` 返回显示名；
3. 用 `Pickup_SmallBox.prefab` 复制改 itemId 即可复用整套拾取与物品栏。

---

## 6. 架构与需求对应

| 需求 | 实现 |
| ---- | ---- |
| 稳定 ItemId | `ItemId` 枚举，显式数值，None=0 保留；演进规则写入注释 |
| 当前用 List\<ItemId\> 即可 | `Inventory` 内部 `List<ItemId>`，无数量/堆叠模型 |
| 默认容量 3、每种任务道具最多一件 | `capacity = 3` + `Contains` 去重 |
| Inventory 不依赖场景 GameObject | 只存 `ItemId`，无任何场景引用 |
| 先添加成功才隐藏 | `PickupItem.Interact`：`TryAddItem` 成功后才 `SetActive(false)` |
| 添加失败物件保留 | 失败路径不隐藏、不改状态，控制台输出原因 |
| PickupItem 不负责章节完成判断 | 无章节引用，只做“入背包 + 隐藏” |
| 物品栏 UI 只读展示、不决定能否拾取 | `InventoryHUD` 只读 `Inventory.Items` 并渲染 |
| 事件通知代替每帧刷新 | `Inventory.ItemAdded / ItemRemoved / Changed`，HUD 订阅 `Changed` |
| 连续按 E / 同物重复 / 满包 / 双同 Id / 移开视角 | 见 4.2 逐条覆盖 |

---

## 7. 验证情况

- **编译**：Unity 6000.6.0f1 batchmode 全量导入，`Assembly-CSharp` 0 错误 0 警告。
- **场景配置**：批量驱动 `InventorySystemSetup.Setup()` 并断言 15 项全 PASS（Player+Inventory 容量 3、小盒 + PickupItem + itemId=SmallBox + Interactable 层 + Collider、UI_Inventory + HUD 接线 + 3 槽全部绑定、预制体与材质落盘），场景已保存。
- **运行时验收**：Play 模式 + `InputTestRuntime` 驱动 E 键（射线、目标管理、拾取顺序、事件、HUD 刷新均为生产代码真实执行），**15/15 PASS**，详见根目录 `拾取系统_验收日志.md`：
  - 拾取主流程：对准 → E → 背包出现 `SmallBox` → 小盒消失 → 槽 1 更新；
  - 边界：连续按 E 不重复、同物重复拾取被拒、移开视角、两个相同 Id 物件第二盒被拒且保留、背包满拒绝添加、全程无 Error/Exception、**全程只增加一件物品**。
- 验证缺口说明：真正“背包已满”分支需两种不同物品才能自然填满，自动化以容量置 0 触发该分支；完整复现步骤见 4.2 附注。

---

## 8. 暂不实现（明确范围外）

拖拽背包、物品排序、丢弃物品、物品组合、物品详细介绍页、3D 手持物件、存档/读档、**放置系统（ItemPlacement 不实现）**。
